module Header {
	public import from Parameters all;
	public import from PhyIOAUX all;
	public import from Components all;
	public import from Functions all;

	//** default altstep
	altstep alt_default() runs on me{
		[] t.timeout {
			setverdict(inconc);
		}
		[] any port.receive {
			setverdict(inconc);
		}
	}
}