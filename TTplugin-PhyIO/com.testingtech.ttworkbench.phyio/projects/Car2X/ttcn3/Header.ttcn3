module Header {
	public import from PhyIOAUX all;
	public import from Components all;

	//** default altstep
	altstep alt_default() runs on me{
		[] t.timeout {
			setverdict(inconc);
		}
		[] any port.receive {
			setverdict(inconc);
		}
	}
}