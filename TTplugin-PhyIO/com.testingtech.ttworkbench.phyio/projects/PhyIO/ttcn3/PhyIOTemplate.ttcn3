module PhyIOTemplate {
	
	
	import from PhyIOConfig all;
	
	group sending {
		template SerialMessage m_sendSerial(integer p_deviceID, integer p_moduleID, Params p_params) := {
			deviceID := p_deviceID,
		    moduleID := p_moduleID,
			params := p_params
		}
		
	}
	
	
}