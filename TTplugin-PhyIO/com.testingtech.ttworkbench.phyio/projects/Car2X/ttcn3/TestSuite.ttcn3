module TestSuite {
	import from Header all;
	
	import from Lights all;
	import from Doors all;
	import from RFIDModule all;
	import from TheftDetection all;
	import from Distance all;
	
	control {
		executeLightsTestcases();
		executeDoorTestcases();
		executeRFIDTestcases();
		executeTheftDetectionTestcases();
		executeDistanceTestcases();
	}
}